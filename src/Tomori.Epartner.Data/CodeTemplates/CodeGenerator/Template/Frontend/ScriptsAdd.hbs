//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    $('#{{name}}-addBtn').on('click', function () {
        $('.clear').val('');
        $('#md-{{name}}-add').modal('show');
        $('#{{name}}-create_btn').unbind();
        $('#{{name}}-create_btn').on('click', function () {
            add{{name}}Save();
        });
    });
});

function add{{name}}Save() {
    ConfirmMessage('Apakah Anda Yakin?', function (isConfirm) {
        if (isConfirm) {
            var param = {
{{attributes}}
            }
            RequestData('POST', `/v1/{{name}}/add`, '#md-{{name}}-add', null, JSON.stringify(param), function (data_obj) {
                if (data_obj.succeeded) {
                    ShowNotif("Data Berhasil Disimpan", "success");
                    $('#md-{{name}}-add').modal('hide');
                    getList{{name}}();
                }
                else if (data_obj.code == 401) { //unathorized
                    AlertMessage(data_obj.message);
                } else
                    ShowNotif(`${data_obj.message} : ${data_obj.description}`, "error");
            });
        }
    });
}