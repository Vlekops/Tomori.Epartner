//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
// </auto-generated>
//------------------------------------------------------------------------------

using AutoMapper;
using MediatR;
using Vleko.DAL.Interface;
using Microsoft.EntityFrameworkCore;
using System.Linq.Expressions;
using Microsoft.Extensions.Logging;
using Tomori.Epartner.Data;
using Tomori.Epartner.Data.Model;
using Vleko.Result;
using Tomori.Epartner.Core.Response;
using Tomori.Epartner.Core.Helper;

namespace Tomori.Epartner.Core.ChangeLogProperty.Query
{
    public class GetChangeLogPropertyListRequest : ListRequest,IListRequest<GetChangeLogPropertyListRequest>,IRequest<ListResponse<ChangeLogPropertyResponse>>
    {
    }
    internal class GetChangeLogPropertyListHandler : IRequestHandler<GetChangeLogPropertyListRequest, ListResponse<ChangeLogPropertyResponse>>
    {
        private readonly ILogger _logger;
        private readonly IMapper _mapper;
        private readonly IUnitOfWork<ApplicationDBContext> _context;
        public GetChangeLogPropertyListHandler(
            ILogger<GetChangeLogPropertyListHandler> logger,
            IMapper mapper,
            IUnitOfWork<ApplicationDBContext> context
            )
        {
            _logger = logger;
            _mapper = mapper;
            _context = context;
        }

        public async Task<ListResponse<ChangeLogPropertyResponse>> Handle(GetChangeLogPropertyListRequest request, CancellationToken cancellationToken)
        {
            ListResponse<ChangeLogPropertyResponse> result = new ListResponse<ChangeLogPropertyResponse>();
            try
            {
				var query = _context.Entity<Tomori.Epartner.Data.Model.ChangeLogProperty>().AsQueryable();

				#region Filter
				Expression<Func<Tomori.Epartner.Data.Model.ChangeLogProperty, object>> column_sort = null;
				List<Expression<Func<Tomori.Epartner.Data.Model.ChangeLogProperty, bool>>> where = new List<Expression<Func<Tomori.Epartner.Data.Model.ChangeLogProperty, bool>>>();
				if (request.Filter != null && request.Filter.Count > 0)
				{
					foreach (var f in request.Filter)
					{
						var obj = ListExpression(f.Search, f.Field, true);
						if (obj.where != null)
							where.Add(obj.where);
					}
				}
				if (where != null && where.Count() > 0)
				{
					foreach (var w in where)
					{
						query = query.Where(w);
					}
				}
				if (request.Sort != null)
                {
					column_sort = ListExpression(request.Sort.Field, request.Sort.Field, false).order!;
					if(column_sort != null)
						query = request.Sort.Type == SortTypeEnum.ASC ? query.OrderBy(column_sort) : query.OrderByDescending(column_sort);
					else
						query = query.OrderBy(d=>d.Id);
				}
				#endregion

				var query_count = query;
				if (request.Start.HasValue && request.Length.HasValue && request.Length > 0)
					query = query.Skip((request.Start.Value - 1) * request.Length.Value).Take(request.Length.Value);
				var data_list = await query.ToListAsync();

				result.List = _mapper.Map<List<ChangeLogPropertyResponse>>(data_list);
				result.Filtered = data_list.Count();
				result.Count = await query_count.CountAsync();
				result.OK();
            }
            catch (Exception ex)
            {
                _logger.LogError(ex, "Failed Get List ChangeLogProperty", request);
                result.Error("Failed Get List ChangeLogProperty", ex.Message);
            }
            return result;
        }

        #region List Utility
		private (Expression<Func<Tomori.Epartner.Data.Model.ChangeLogProperty, bool>> where, Expression<Func<Tomori.Epartner.Data.Model.ChangeLogProperty, object>> order) ListExpression(string search, string field, bool is_where)
		{
			Expression<Func<Tomori.Epartner.Data.Model.ChangeLogProperty, object>> result_order = null;
			Expression<Func<Tomori.Epartner.Data.Model.ChangeLogProperty, bool>> result_where = null;
            if (!string.IsNullOrWhiteSpace(search) && !string.IsNullOrWhiteSpace(field))
            {
                field = field.Trim().ToLower();
                search = search.Trim().ToLower();
                switch (field)
                {
					case "id" : 
						if(is_where){
							if (Guid.TryParse(search, out var _Id))
								result_where = (d=>d.Id == _Id);
								else
								result_where = (d=>d.Id == Guid.Empty);
						}
						else
							result_order = (d => d.Id);
					break;
					case "idchangelog" : 
						if(is_where){
							if (Guid.TryParse(search, out var _IdChangeLog))
								result_where = (d=>d.IdChangeLog == _IdChangeLog);
								else
								result_where = (d=>d.IdChangeLog == Guid.Empty);
						}
						else
							result_order = (d => d.IdChangeLog);
					break;
					case "newvalue" : 
						if(is_where){
							result_where = (d=>d.NewValue.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.NewValue);
					break;
					case "oldvalue" : 
						if(is_where){
							result_where = (d=>d.OldValue.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.OldValue);
					break;
					case "property" : 
						if(is_where){
							result_where = (d=>d.Property.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.Property);
					break;

                }
            }
            return (result_where, result_order);
        }
        #endregion
    }
}

