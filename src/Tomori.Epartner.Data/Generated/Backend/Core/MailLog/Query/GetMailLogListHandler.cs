//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated from a template.
//     Manual changes to this file will be overwritten if the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------

using AutoMapper;
using MediatR;
using Vleko.DAL.Interface;
using Microsoft.EntityFrameworkCore;
using System.Linq.Expressions;
using Microsoft.Extensions.Logging;
using Tomori.Epartner.Data;
using Tomori.Epartner.Data.Model;
using Vleko.Result;
using Tomori.Epartner.Core.Response;
using Tomori.Epartner.Core.Helper;

namespace Tomori.Epartner.Core.MailLog.Query
{
    public class GetMailLogListRequest : ListRequest,IListRequest<GetMailLogListRequest>,IRequest<ListResponse<MailLogResponse>>
    {
    }
    internal class GetMailLogListHandler : IRequestHandler<GetMailLogListRequest, ListResponse<MailLogResponse>>
    {
        private readonly ILogger _logger;
        private readonly IMapper _mapper;
        private readonly IUnitOfWork<ApplicationDBContext> _context;
        public GetMailLogListHandler(
            ILogger<GetMailLogListHandler> logger,
            IMapper mapper,
            IUnitOfWork<ApplicationDBContext> context
            )
        {
            _logger = logger;
            _mapper = mapper;
            _context = context;
        }

        public async Task<ListResponse<MailLogResponse>> Handle(GetMailLogListRequest request, CancellationToken cancellationToken)
        {
            ListResponse<MailLogResponse> result = new ListResponse<MailLogResponse>();
            try
            {
				var query = _context.Entity<Tomori.Epartner.Data.Model.MailLog>().AsQueryable();

				#region Filter
				Expression<Func<Tomori.Epartner.Data.Model.MailLog, object>> column_sort = null;
				List<Expression<Func<Tomori.Epartner.Data.Model.MailLog, bool>>> where = new List<Expression<Func<Tomori.Epartner.Data.Model.MailLog, bool>>>();
				if (request.Filter != null && request.Filter.Count > 0)
				{
					foreach (var f in request.Filter)
					{
						var obj = ListExpression(f.Search, f.Field, true);
						if (obj.where != null)
							where.Add(obj.where);
					}
				}
				if (where != null && where.Count() > 0)
				{
					foreach (var w in where)
					{
						query = query.Where(w);
					}
				}
				if (request.Sort != null)
                {
					column_sort = ListExpression(request.Sort.Field, request.Sort.Field, false).order!;
					if(column_sort != null)
						query = request.Sort.Type == SortTypeEnum.ASC ? query.OrderBy(column_sort) : query.OrderByDescending(column_sort);
					else
						query = query.OrderBy(d=>d.Id);
				}
				#endregion

				var query_count = query;
				if (request.Start.HasValue && request.Length.HasValue && request.Length > 0)
					query = query.Skip((request.Start.Value - 1) * request.Length.Value).Take(request.Length.Value);
				var data_list = await query.ToListAsync();

				result.List = _mapper.Map<List<MailLogResponse>>(data_list);
				result.Filtered = data_list.Count();
				result.Count = await query_count.CountAsync();
				result.OK();
            }
            catch (Exception ex)
            {
                _logger.LogError(ex, "Failed Get List MailLog", request);
                result.Error("Failed Get List MailLog", ex.Message);
            }
            return result;
        }

        #region List Utility
		private (Expression<Func<Tomori.Epartner.Data.Model.MailLog, bool>> where, Expression<Func<Tomori.Epartner.Data.Model.MailLog, object>> order) ListExpression(string search, string field, bool is_where)
		{
			Expression<Func<Tomori.Epartner.Data.Model.MailLog, object>> result_order = null;
			Expression<Func<Tomori.Epartner.Data.Model.MailLog, bool>> result_where = null;
            if (!string.IsNullOrWhiteSpace(search) && !string.IsNullOrWhiteSpace(field))
            {
                field = field.Trim().ToLower();
                search = search.Trim().ToLower();
                switch (field)
                {
					case "id" : 
						if(is_where){
							if (Guid.TryParse(search, out var _Id))
								result_where = (d=>d.Id == _Id);
								else
								result_where = (d=>d.Id == Guid.Empty);
						}
						else
							result_order = (d => d.Id);
					break;
					case "bodymail" : 
						if(is_where){
							result_where = (d=>d.BodyMail.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.BodyMail);
					break;
					case "ccmail" : 
						if(is_where){
							result_where = (d=>d.CcMail.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.CcMail);
					break;
					case "createdate" : 
						if(is_where){
							if (DateTime.TryParse(search, out var _CreateDate))
								result_where = (d=>d.CreateDate == _CreateDate);
						}
						else
							result_order = (d => d.CreateDate);
					break;
					case "iduser" : 
						if(is_where){
							if (Guid.TryParse(search, out var _IdUser))
								result_where = (d=>d.IdUser == _IdUser);
								else
								result_where = (d=>d.IdUser == Guid.Empty);
						}
						else
							result_order = (d => d.IdUser);
					break;
					case "sendermail" : 
						if(is_where){
							result_where = (d=>d.SenderMail.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.SenderMail);
					break;
					case "subject" : 
						if(is_where){
							result_where = (d=>d.Subject.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.Subject);
					break;
					case "tomail" : 
						if(is_where){
							result_where = (d=>d.ToMail.Trim().ToLower().Contains(search));
						}
						else
							result_order = (d => d.ToMail);
					break;

                }
            }
            return (result_where, result_order);
        }
        #endregion
    }
}

