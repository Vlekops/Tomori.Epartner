//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    getListWorkflow();
});

function getListWorkflow(page) {
    page = page != undefined ? page : 1;
    var pageSize = $('#Workflow-page_select').val();
    var element = {
        table: '#Workflow-table',
        tbody: '#Workflow-tbody',
        from: '#Workflow-from_page',
        to: '#Workflow-to_page',
        total: '#Workflow-total',
        pagination: '#Workflow-pagination',
        item_pagination: 'Workflow-item'
    }
    var param = {
        sort: {
            field: "id",
            type: 0
        },
        start: page,
        length: pageSize
    }
    RequestData('POST', "/v1/Workflow/list", element.table, element.tbody, JSON.stringify(param), function (data) {
        if (data.succeeded) {
            $(element.tbody).html('');
            $(element.pagination).html('');
            if (data.list.length > 0) {
                SetTableData(true, 12, element, {
                    page: page,
                    pageSize: pageSize,
                    count: data.count,
                    function_name: 'getListWorkflow'
                }, function (count) {
                    data.list.forEach(function (item) {
                        $(element.tbody).append(`
                            <tr>
                                <td class="text-nowrap text-center">${count}</td>
								<td class="text-nowrap">${item.callbackUrl}</td>
								<td class="text-nowrap">${item.code}</td>
								<td class="text-nowrap">${item.dataWorkflow}</td>
								<td class="text-nowrap">${item.description}</td>
								<td class="text-nowrap">${item.documentNo}</td>
								<td class="text-nowrap">${item.emailRequester}</td>
								<td class="text-nowrap">${item.fullnameRequester}</td>
								<td class="text-nowrap">${item.groupNo}</td>
								<td class="text-nowrap">${item.idUserRequester}</td>
								<td class="text-nowrap">${item.navigationUrl}</td>
								<td class="text-nowrap">${item.statusCode}</td>
								<td class="text-nowrap">${item.statusDescription}</td>
								<td class="text-nowrap">${item.stepNo}</td>
								<td class="text-nowrap">${item.subject}</td>
								<td class="text-nowrap">${item.workflowCode}</td>

                                <td class="text-center">
                                    <div>
                                        <button type="button" data-toggle="dropdown" class="btn btn-sm btn-info dropdown-toggle" aria-hashpopup="true" aria-expanded="false">Aksi</button>
                                        <div class="dropdown-menu">
                                            <a href="#!" class="dropdown-item" onclick="detailWorkflowDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Detail</a>
                                            <a href="#!" class="dropdown-item" onclick="editWorkflowDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Edit</a>
                                            <a href="#!" class="dropdown-item" onclick="deleteWorkflowDialog(${item.id})">Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `);
                        count++;
                    });
                });
            } else {
                settabledata(false, 12, element);
            }
        } else {
            ShowNotif(`${data.message} : ${data.description}`, "error");
        }
    })
}

