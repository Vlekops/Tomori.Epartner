//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    getListUser();
});

function getListUser(page) {
    page = page != undefined ? page : 1;
    var pageSize = $('#User-page_select').val();
    var element = {
        table: '#User-table',
        tbody: '#User-tbody',
        from: '#User-from_page',
        to: '#User-to_page',
        total: '#User-total',
        pagination: '#User-pagination',
        item_pagination: 'User-item'
    }
    var param = {
        sort: {
            field: "id",
            type: 0
        },
        start: page,
        length: pageSize
    }
    RequestData('POST', "/v1/User/list", element.table, element.tbody, JSON.stringify(param), function (data) {
        if (data.succeeded) {
            $(element.tbody).html('');
            $(element.pagination).html('');
            if (data.list.length > 0) {
                SetTableData(true, 12, element, {
                    page: page,
                    pageSize: pageSize,
                    count: data.count,
                    function_name: 'getListUser'
                }, function (count) {
                    data.list.forEach(function (item) {
                        $(element.tbody).append(`
                            <tr>
                                <td class="text-nowrap text-center">${count}</td>
								<td class="text-nowrap">${item.accessFailedCount}</td>
								<td class="text-nowrap">${item.active}</td>
								<td class="text-nowrap">${item.expiredPassword}</td>
								<td class="text-nowrap">${item.expiredUser}</td>
								<td class="text-nowrap">${item.fullname}</td>
								<td class="text-nowrap">${item.isLockout}</td>
								<td class="text-nowrap">${item.lastChangePassword}</td>
								<td class="text-nowrap">${item.lastLogin}</td>
								<td class="text-nowrap">${item.mail}</td>
								<td class="text-nowrap">${item.password}</td>
								<td class="text-nowrap">${item.phoneNumber}</td>
								<td class="text-nowrap">${item.photoUrl}</td>
								<td class="text-nowrap">${item.token}</td>
								<td class="text-nowrap">${item.username}</td>

                                <td class="text-center">
                                    <div>
                                        <button type="button" data-toggle="dropdown" class="btn btn-sm btn-info dropdown-toggle" aria-hashpopup="true" aria-expanded="false">Aksi</button>
                                        <div class="dropdown-menu">
                                            <a href="#!" class="dropdown-item" onclick="detailUserDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Detail</a>
                                            <a href="#!" class="dropdown-item" onclick="editUserDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Edit</a>
                                            <a href="#!" class="dropdown-item" onclick="deleteUserDialog(${item.id})">Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `);
                        count++;
                    });
                });
            } else {
                settabledata(false, 12, element);
            }
        } else {
            ShowNotif(`${data.message} : ${data.description}`, "error");
        }
    })
}

