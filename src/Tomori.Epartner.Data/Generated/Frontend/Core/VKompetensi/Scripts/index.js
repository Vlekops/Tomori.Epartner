//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    getListVKompetensi();
});

function getListVKompetensi(page) {
    page = page != undefined ? page : 1;
    var pageSize = $('#VKompetensi-page_select').val();
    var element = {
        table: '#VKompetensi-table',
        tbody: '#VKompetensi-tbody',
        from: '#VKompetensi-from_page',
        to: '#VKompetensi-to_page',
        total: '#VKompetensi-total',
        pagination: '#VKompetensi-pagination',
        item_pagination: 'VKompetensi-item'
    }
    var param = {
        sort: {
            field: "id",
            type: 0
        },
        start: page,
        length: pageSize
    }
    RequestData('POST', "/v1/VKompetensi/list", element.table, element.tbody, JSON.stringify(param), function (data) {
        if (data.succeeded) {
            $(element.tbody).html('');
            $(element.pagination).html('');
            if (data.list.length > 0) {
                SetTableData(true, 12, element, {
                    page: page,
                    pageSize: pageSize,
                    count: data.count,
                    function_name: 'getListVKompetensi'
                }, function (count) {
                    data.list.forEach(function (item) {
                        $(element.tbody).append(`
                            <tr>
                                <td class="text-nowrap text-center">${count}</td>
								<td class="text-nowrap">${item.bidangSubBidang}</td>
								<td class="text-nowrap">${item.bidangSubBidangCode}</td>
								<td class="text-nowrap">${item.civdId}</td>
								<td class="text-nowrap">${item.completedDate}</td>
								<td class="text-nowrap">${item.deskripsi}</td>
								<td class="text-nowrap">${item.document}</td>
								<td class="text-nowrap">${item.documentId}</td>
								<td class="text-nowrap">${item.idVendor}</td>
								<td class="text-nowrap">${item.jenisMataUang}</td>
								<td class="text-nowrap">${item.nilaiKontrakPoso}</td>
								<td class="text-nowrap">${item.noKontrakPoso}</td>
								<td class="text-nowrap">${item.perusahaan}</td>
								<td class="text-nowrap">${item.progressKontrakPoso}</td>
								<td class="text-nowrap">${item.tglKontrakPoso}</td>
								<td class="text-nowrap">${item.tglPenyelesaian}</td>

                                <td class="text-center">
                                    <div>
                                        <button type="button" data-toggle="dropdown" class="btn btn-sm btn-info dropdown-toggle" aria-hashpopup="true" aria-expanded="false">Aksi</button>
                                        <div class="dropdown-menu">
                                            <a href="#!" class="dropdown-item" onclick="detailVKompetensiDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Detail</a>
                                            <a href="#!" class="dropdown-item" onclick="editVKompetensiDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Edit</a>
                                            <a href="#!" class="dropdown-item" onclick="deleteVKompetensiDialog(${item.id})">Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `);
                        count++;
                    });
                });
            } else {
                settabledata(false, 12, element);
            }
        } else {
            ShowNotif(`${data.message} : ${data.description}`, "error");
        }
    })
}

