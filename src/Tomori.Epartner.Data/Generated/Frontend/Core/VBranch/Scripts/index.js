//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    getListVBranch();
});

function getListVBranch(page) {
    page = page != undefined ? page : 1;
    var pageSize = $('#VBranch-page_select').val();
    var element = {
        table: '#VBranch-table',
        tbody: '#VBranch-tbody',
        from: '#VBranch-from_page',
        to: '#VBranch-to_page',
        total: '#VBranch-total',
        pagination: '#VBranch-pagination',
        item_pagination: 'VBranch-item'
    }
    var param = {
        sort: {
            field: "id",
            type: 0
        },
        start: page,
        length: pageSize
    }
    RequestData('POST', "/v1/VBranch/list", element.table, element.tbody, JSON.stringify(param), function (data) {
        if (data.succeeded) {
            $(element.tbody).html('');
            $(element.pagination).html('');
            if (data.list.length > 0) {
                SetTableData(true, 12, element, {
                    page: page,
                    pageSize: pageSize,
                    count: data.count,
                    function_name: 'getListVBranch'
                }, function (count) {
                    data.list.forEach(function (item) {
                        $(element.tbody).append(`
                            <tr>
                                <td class="text-nowrap text-center">${count}</td>
								<td class="text-nowrap">${item.address}</td>
								<td class="text-nowrap">${item.civdId}</td>
								<td class="text-nowrap">${item.companyType}</td>
								<td class="text-nowrap">${item.completedDate}</td>
								<td class="text-nowrap">${item.contactPerson}</td>
								<td class="text-nowrap">${item.faxNumber}</td>
								<td class="text-nowrap">${item.idVendor}</td>
								<td class="text-nowrap">${item.npwp}</td>
								<td class="text-nowrap">${item.phoneNumber}</td>
								<td class="text-nowrap">${item.pkp}</td>
								<td class="text-nowrap">${item.situ}</td>
								<td class="text-nowrap">${item.vendorBranchName}</td>
								<td class="text-nowrap">${item.vendorEmail1}</td>
								<td class="text-nowrap">${item.vendorEmail2}</td>
								<td class="text-nowrap">${item.zipCode}</td>

                                <td class="text-center">
                                    <div>
                                        <button type="button" data-toggle="dropdown" class="btn btn-sm btn-info dropdown-toggle" aria-hashpopup="true" aria-expanded="false">Aksi</button>
                                        <div class="dropdown-menu">
                                            <a href="#!" class="dropdown-item" onclick="detailVBranchDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Detail</a>
                                            <a href="#!" class="dropdown-item" onclick="editVBranchDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Edit</a>
                                            <a href="#!" class="dropdown-item" onclick="deleteVBranchDialog(${item.id})">Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `);
                        count++;
                    });
                });
            } else {
                settabledata(false, 12, element);
            }
        } else {
            ShowNotif(`${data.message} : ${data.description}`, "error");
        }
    })
}

