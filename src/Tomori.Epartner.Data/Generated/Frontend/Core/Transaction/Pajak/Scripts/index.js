//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    getListPajak();
});

function getListPajak(page) {
    page = page != undefined ? page : 1;
    var pageSize = $('#Pajak-page_select').val();
    var element = {
        table: '#Pajak-table',
        tbody: '#Pajak-tbody',
        from: '#Pajak-from_page',
        to: '#Pajak-to_page',
        total: '#Pajak-total',
        pagination: '#Pajak-pagination',
        item_pagination: 'Pajak-item'
    }
    var param = {
        sort: {
            field: "id",
            type: 0
        },
        start: page,
        length: pageSize
    }
    RequestData('POST', "/v1/Pajak/list", element.table, element.tbody, JSON.stringify(param), function (data) {
        if (data.succeeded) {
            $(element.tbody).html('');
            $(element.pagination).html('');
            if (data.list.length > 0) {
                SetTableData(true, 12, element, {
                    page: page,
                    pageSize: pageSize,
                    count: data.count,
                    function_name: 'getListPajak'
                }, function (count) {
                    data.list.forEach(function (item) {
                        $(element.tbody).append(`
                            <tr>
                                <td class="text-nowrap text-center">${count}</td>
								<td class="text-nowrap">${item.fileDokumen}</td>
								<td class="text-nowrap">${item.fileDokumenId}</td>
								<td class="text-nowrap">${item.kondisi}</td>
								<td class="text-nowrap">${item.noDokumen}</td>
								<td class="text-nowrap">${item.periodeAkhir}</td>
								<td class="text-nowrap">${item.periodeAwal}</td>
								<td class="text-nowrap">${item.tahun}</td>
								<td class="text-nowrap">${item.tanggal}</td>
								<td class="text-nowrap">${item.tanggalAkhir}</td>
								<td class="text-nowrap">${item.tipeDokumen}</td>
								<td class="text-nowrap">${item.vendorId}</td>

                                <td class="text-center">
                                    <div>
                                        <button type="button" data-toggle="dropdown" class="btn btn-sm btn-info dropdown-toggle" aria-hashpopup="true" aria-expanded="false">Aksi</button>
                                        <div class="dropdown-menu">
                                            <a href="#!" class="dropdown-item" onclick="detailPajakDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Detail</a>
                                            <a href="#!" class="dropdown-item" onclick="editPajakDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Edit</a>
                                            <a href="#!" class="dropdown-item" onclick="deletePajakDialog(${item.id})">Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `);
                        count++;
                    });
                });
            } else {
                settabledata(false, 12, element);
            }
        } else {
            ShowNotif(`${data.message} : ${data.description}`, "error");
        }
    })
}

