//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    getListRekeningBank();
});

function getListRekeningBank(page) {
    page = page != undefined ? page : 1;
    var pageSize = $('#RekeningBank-page_select').val();
    var element = {
        table: '#RekeningBank-table',
        tbody: '#RekeningBank-tbody',
        from: '#RekeningBank-from_page',
        to: '#RekeningBank-to_page',
        total: '#RekeningBank-total',
        pagination: '#RekeningBank-pagination',
        item_pagination: 'RekeningBank-item'
    }
    var param = {
        sort: {
            field: "id",
            type: 0
        },
        start: page,
        length: pageSize
    }
    RequestData('POST', "/v1/RekeningBank/list", element.table, element.tbody, JSON.stringify(param), function (data) {
        if (data.succeeded) {
            $(element.tbody).html('');
            $(element.pagination).html('');
            if (data.list.length > 0) {
                SetTableData(true, 12, element, {
                    page: page,
                    pageSize: pageSize,
                    count: data.count,
                    function_name: 'getListRekeningBank'
                }, function (count) {
                    data.list.forEach(function (item) {
                        $(element.tbody).append(`
                            <tr>
                                <td class="text-nowrap text-center">${count}</td>
								<td class="text-nowrap">${item.fileSuratPernyataan}</td>
								<td class="text-nowrap">${item.fileSuratPernyataanId}</td>
								<td class="text-nowrap">${item.jenisMataUang}</td>
								<td class="text-nowrap">${item.kantorCabang}</td>
								<td class="text-nowrap">${item.namaBank}</td>
								<td class="text-nowrap">${item.negara}</td>
								<td class="text-nowrap">${item.noRekening}</td>
								<td class="text-nowrap">${item.noRekeningFormat}</td>
								<td class="text-nowrap">${item.pemegangRekening}</td>
								<td class="text-nowrap">${item.vendorId}</td>

                                <td class="text-center">
                                    <div>
                                        <button type="button" data-toggle="dropdown" class="btn btn-sm btn-info dropdown-toggle" aria-hashpopup="true" aria-expanded="false">Aksi</button>
                                        <div class="dropdown-menu">
                                            <a href="#!" class="dropdown-item" onclick="detailRekeningBankDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Detail</a>
                                            <a href="#!" class="dropdown-item" onclick="editRekeningBankDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Edit</a>
                                            <a href="#!" class="dropdown-item" onclick="deleteRekeningBankDialog(${item.id})">Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `);
                        count++;
                    });
                });
            } else {
                settabledata(false, 12, element);
            }
        } else {
            ShowNotif(`${data.message} : ${data.description}`, "error");
        }
    })
}

