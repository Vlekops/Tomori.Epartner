//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    getListIzinUsaha();
});

function getListIzinUsaha(page) {
    page = page != undefined ? page : 1;
    var pageSize = $('#IzinUsaha-page_select').val();
    var element = {
        table: '#IzinUsaha-table',
        tbody: '#IzinUsaha-tbody',
        from: '#IzinUsaha-from_page',
        to: '#IzinUsaha-to_page',
        total: '#IzinUsaha-total',
        pagination: '#IzinUsaha-pagination',
        item_pagination: 'IzinUsaha-item'
    }
    var param = {
        sort: {
            field: "id",
            type: 0
        },
        start: page,
        length: pageSize
    }
    RequestData('POST', "/v1/IzinUsaha/list", element.table, element.tbody, JSON.stringify(param), function (data) {
        if (data.succeeded) {
            $(element.tbody).html('');
            $(element.pagination).html('');
            if (data.list.length > 0) {
                SetTableData(true, 12, element, {
                    page: page,
                    pageSize: pageSize,
                    count: data.count,
                    function_name: 'getListIzinUsaha'
                }, function (count) {
                    data.list.forEach(function (item) {
                        $(element.tbody).append(`
                            <tr>
                                <td class="text-nowrap text-center">${count}</td>
								<td class="text-nowrap">${item.akhirBerlaku}</td>
								<td class="text-nowrap">${item.bidangUsaha}</td>
								<td class="text-nowrap">${item.fileIzinUsaha}</td>
								<td class="text-nowrap">${item.fileIzinUsahaId}</td>
								<td class="text-nowrap">${item.golonganUsaha}</td>
								<td class="text-nowrap">${item.instansiPemberiIzin}</td>
								<td class="text-nowrap">${item.jenisIzinUsaha}</td>
								<td class="text-nowrap">${item.jenisMataUang}</td>
								<td class="text-nowrap">${item.kekayaanBershi}</td>
								<td class="text-nowrap">${item.merkStp}</td>
								<td class="text-nowrap">${item.mulaiBerlaku}</td>
								<td class="text-nowrap">${item.noIzinUsaha}</td>
								<td class="text-nowrap">${item.other}</td>
								<td class="text-nowrap">${item.peringkatInspeksi}</td>
								<td class="text-nowrap">${item.tipeStp}</td>
								<td class="text-nowrap">${item.vendorId}</td>

                                <td class="text-center">
                                    <div>
                                        <button type="button" data-toggle="dropdown" class="btn btn-sm btn-info dropdown-toggle" aria-hashpopup="true" aria-expanded="false">Aksi</button>
                                        <div class="dropdown-menu">
                                            <a href="#!" class="dropdown-item" onclick="detailIzinUsahaDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Detail</a>
                                            <a href="#!" class="dropdown-item" onclick="editIzinUsahaDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Edit</a>
                                            <a href="#!" class="dropdown-item" onclick="deleteIzinUsahaDialog(${item.id})">Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `);
                        count++;
                    });
                });
            } else {
                settabledata(false, 12, element);
            }
        } else {
            ShowNotif(`${data.message} : ${data.description}`, "error");
        }
    })
}

