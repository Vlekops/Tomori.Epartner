//------------------------------------------------------------------------------
//<auto-generated>
//    This code was generated from a template.
//    Manual changes to this file will be overwritten if the code is regenerated.
//</auto-generated>
//------------------------------------------------------------------------------

$(document).ready(function () {
    getListWorkflowLog();
});

function getListWorkflowLog(page) {
    page = page != undefined ? page : 1;
    var pageSize = $('#WorkflowLog-page_select').val();
    var element = {
        table: '#WorkflowLog-table',
        tbody: '#WorkflowLog-tbody',
        from: '#WorkflowLog-from_page',
        to: '#WorkflowLog-to_page',
        total: '#WorkflowLog-total',
        pagination: '#WorkflowLog-pagination',
        item_pagination: 'WorkflowLog-item'
    }
    var param = {
        sort: {
            field: "id",
            type: 0
        },
        start: page,
        length: pageSize
    }
    RequestData('POST', "/v1/WorkflowLog/list", element.table, element.tbody, JSON.stringify(param), function (data) {
        if (data.succeeded) {
            $(element.tbody).html('');
            $(element.pagination).html('');
            if (data.list.length > 0) {
                SetTableData(true, 12, element, {
                    page: page,
                    pageSize: pageSize,
                    count: data.count,
                    function_name: 'getListWorkflowLog'
                }, function (count) {
                    data.list.forEach(function (item) {
                        $(element.tbody).append(`
                            <tr>
                                <td class="text-nowrap text-center">${count}</td>
								<td class="text-nowrap">${item.email}</td>
								<td class="text-nowrap">${item.fullName}</td>
								<td class="text-nowrap">${item.groupNo}</td>
								<td class="text-nowrap">${item.idUser}</td>
								<td class="text-nowrap">${item.idWorkflow}</td>
								<td class="text-nowrap">${item.idWorkflowStatus}</td>
								<td class="text-nowrap">${item.isAdhoc}</td>
								<td class="text-nowrap">${item.isReviewer}</td>
								<td class="text-nowrap">${item.notes}</td>
								<td class="text-nowrap">${item.statusDescription}</td>
								<td class="text-nowrap">${item.stepName}</td>
								<td class="text-nowrap">${item.stepNo}</td>

                                <td class="text-center">
                                    <div>
                                        <button type="button" data-toggle="dropdown" class="btn btn-sm btn-info dropdown-toggle" aria-hashpopup="true" aria-expanded="false">Aksi</button>
                                        <div class="dropdown-menu">
                                            <a href="#!" class="dropdown-item" onclick="detailWorkflowLogDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Detail</a>
                                            <a href="#!" class="dropdown-item" onclick="editWorkflowLogDialog(this);" data-detail='${JSON.stringify(item).replace(/' /g, " " ) }'>Edit</a>
                                            <a href="#!" class="dropdown-item" onclick="deleteWorkflowLogDialog(${item.id})">Delete</a>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `);
                        count++;
                    });
                });
            } else {
                settabledata(false, 12, element);
            }
        } else {
            ShowNotif(`${data.message} : ${data.description}`, "error");
        }
    })
}

